const fieldTypes = {
    TEXT: 'TEXT',
    NUMBER: 'NUMBER',
    // TODO Date
}

const APP_METAFIELD_NAMESPACE = 'custom-fields-shop'

const GENERIC_ERROR_MSG = 'Unexpected error. Please try again later.'

const INACTIVE_JWT_MSG = 'Inactive JWT'

export {
    fieldTypes,
    APP_METAFIELD_NAMESPACE,
    GENERIC_ERROR_MSG,
    INACTIVE_JWT_MSG,
}
