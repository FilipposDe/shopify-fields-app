{
    "configurations": [
        {
            "name": "Attach",
            "port": 9229,
            "request": "attach",
            "type": "node",
            "processId": "${command:PickProcess}"
        }
    ]
}
